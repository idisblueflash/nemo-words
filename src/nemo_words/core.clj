(ns nemo-words.core)

(defn -main
  "Entry point invoked by `clj -M -m nemo-words.core`. Prints a greeting.

  Example:
    (-main) ;; prints \"Hello, nemo-words!\""
  [& _args]
  (println "Hello, nemo-words!"))
